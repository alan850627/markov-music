{
  "name": "markov-music",
  "version": "0.0.1",
  "description": "analyze and generate music using markov chains",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js -a topVoiceNoteOnlyOrder1-b -n 100 -i in.mid -o out.mid",
    "analyze": "node src/analyze.js -i in.mid -o out.html "
  },
  "author": "Alan Chen",
  "license": "ISC",
  "dependencies": {
    "@tonejs/midi": "^2.0.7"
  }
}
